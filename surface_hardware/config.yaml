name: Surface Hardware
version: "0.2.6"
slug: surface_hardware
description: Surface Pro hardware monitoring and control

arch:
  - amd64

startup: services
boot: auto
init: false

services:
  - mqtt:want

full_access: true
privileged:
  - SYS_ADMIN
apparmor: false
host_network: true
